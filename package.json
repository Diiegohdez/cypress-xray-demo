{
  "name": "cypress-xray-demo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test:ci": "cypress run --browser chrome --headless --reporter mocha-junit-reporter --reporter-options mochaFile=results/test-[hash].xml"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress": "^15.0.0",
    "junit-merge": "^2.0.0",
    "mocha-junit-reporter": "^2.2.1"
  }
}
